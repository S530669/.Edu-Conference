<!DOCTYPE html>
<html>
<head>
	<title>.Edu Conference</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <link href="/css/home.css" rel="stylesheet" type="text/css"/>
</head>
    <!-- <form method="post" action="/index/adminhomepage">
  <div class="container-fluid">
        <div class="header clearfix">
        <nav class="navbar navbar-inverse">  
    <div class="navbar-header">
      <a class="navbar-brand active" href="attendee">Attendee</a>
      <a class="navbar-brand active" href="presenter">presenter</a>   
      <a class="navbar-brand active" href="adminvendor">vendor</a>
      
      </div>
      <li class="pull-right">
            <a class="navbar-brand active" href="/users/logout">Logout</a>
       </li>
      </nav>  
        </div>
  </div> -->
  
    <body>
     <h2> Vendor</h2>
     <br>
     <br>
        <div class="w3-container">
        <table class="w3-table-all w3-card-4"id="rent"  align="center">
          <tr>
            <th>Company Name</th>
            <th>city</th>
            <th>State</th>
            <th>Zip</th>
            <th>Website URL</th>
            <th>Contact Name</th>
            <th>Contact Title</th>
            <th>Email</th>
            <th>Contact Number</th>
          </tr>
    <% for(let i = 0; i < list.length; i++) { %>
    <tr>
      <td><%= list[i].cname %></td>
      <td><%= list[i].city %></td>
      <td><%= list[i].State %></td>
      <td><%= list[i].Zip %></td>
      <td><%= list[i].URL %></td>
      <td><%= list[i].name %></td>
      <td><%= list[i].Title %></td>
      <td><%= list[i].email %></td>
      <td><%= list[i].contact %></td>
    </tr>
    <% } %>
  </table>
  <div>
    </div> 
</div>
